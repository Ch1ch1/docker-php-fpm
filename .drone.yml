---
kind: pipeline
type: docker
name: Building..
node:
  drone: ch1
steps:
  - name: lint
    image: osshelp/drone-linter
    failure: ignore
    settings:
      dockerfiles:
        - Dockerfile
      markdown_files:
        - README.md
  - name: build & push
    image: plugins/docker
    settings:
      repo: ch1ch1/php-fpm
      tags: [ "8.0.10-prod", "latest" ]
      username: ch1ch1
      password:
        from_secret: dockerhub_token
  - name: notification
    image: plugins/webhook
    settings:
      urls:
        from_secret: webhookgotify
      content_type: application/json
      template: |
        {
         "message": "{{ repo.owner }} as updated {{ repo.name }} with {{ build.status }}.",
         "priority": 4,
         "title": "{{ repo.name }} build"
        }
    when:
      status: [success, failure]

trigger:
  event:
    - push
---
kind: signature
hmac: 569b2ffb576df4a81333d147ebf7f513eac27f6e7eefa4c8f0a636107440c22d

...
