---
kind: pipeline
type: docker
name: Building..
node:
  drone: ch1
  drone: nimi
steps:
  - name: build & push
    image: plugins/docker
    settings:
      repo: ch1ch1/php-fpm
      tags: [ "8.0.15-prod", "latest" ]
      username: ch1ch1
      password:
        from_secret: dockerhub_token
  - name: build & push
    image: plugins/docker
    settings:
      repo: ch1ch1/php-fpm
      tags: [ "7.4.27-prod" ]
      username: ch1ch1
      dockerfile: Dockerfilephp7
      password:
        from_secret: dockerhub_token
  - name: notification
    image: plugins/webhook
    settings:
      urls:
        from_secret: webhookgotify
      content_type: application/json
      template: |
        {
         "message": "{{ repo.owner }} as updated {{ repo.name }} with {{ build.status }}.",
         "priority": 4,
         "title": "{{ repo.name }} build"
        }
    when:
      status: [success, failure]

trigger:
  event:
    - push
---
kind: signature
hmac: f01cb1536e4132266df368d7fadf78cbcfeda41287e90788ed898b7ade3851fb

...
